import type { Config } from 'vike/types';

export default {
  ssr: false,
  prerender: false,
  title: 'Open App — LiftShift Workout Analytics Dashboard',
  description:
    'Open the LiftShift app to import your Hevy, Strong, or Lyfta workout logs. Get muscle heatmaps, PR tracking, plateau detection, and set-by-set coaching feedback — 100% free, runs in your browser.',
} satisfies Config;
