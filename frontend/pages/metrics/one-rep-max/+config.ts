import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: '1RM (One-Rep Max) — Estimate, Formula, Safety Tips | LiftShift',
  description:
    '1RM (one-rep max) in LiftShift: what a 1RM estimate means, how it is calculated, common formulas, limitations, and how to use it safely in training.',
} satisfies Config;
