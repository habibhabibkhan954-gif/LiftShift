import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: 'Supported Workout Apps — Hevy, Strong &amp; Lyfta Import | LiftShift',
  description:
    'Supported workout apps for LiftShift: Hevy, Strong, and Lyfta. Learn import options (CSV or sync) and what data is required.',
} satisfies Config;
